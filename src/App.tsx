import React, { useState } from 'react'; import { StatusBar } from 'expo-status-bar'; import { CameraScreen } from './screens/CameraScreen'; import { RankedMatch } from './screens/RankedMatchScreen'; import { ExerciseType, Player } from './types/exercise'; import { useKeepAwake } from 'expo-keep-awake'; export default function App() { const [exerciseType, setExerciseType] = useState('pushup'); useKeepAwake(); const players = [{ id: '1', name: 'Player 1', avatar: 'avatar1', elo: 1247, rank: 'Silver', badge: 'Badge1' }, { id: '2', name: 'Player 2', avatar: 'avatar2', elo: 4024, rank: 'Grandmaster', badge: 'Badge2' }]; return (<> <StatusBar style='light' backgroundColor='#000' /> {exerciseType === 'ranked' && (<RankedMatch player1={players[0]} player2={players[1]} />)} {!exerciseType === 'ranked' && (<CameraScreen exerciseType={exerciseType} onExerciseChange={setExerciseType} />)} </> ); }
